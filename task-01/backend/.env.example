PORT=5001
# Leave DATABASE_URL empty to use instant in-memory PostgreSQL for testing,
# or set to a live PostgreSQL connection URL (e.g. Neon.tech / Supabase / Render Postgres):
# DATABASE_URL=postgresql://user:password@ep-something.neon.tech/neondb?sslmode=require
DATABASE_URL=
DB_SSL=true
